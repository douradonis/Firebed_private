services:
  - type: web
    name: mydatatest
    env: docker
    repo: https://github.com/yourusername/YourRepo
    branch: main
    plan: free
    buildCommand: docker build -t registry:latest .
    startCommand: ""  # Render will use Dockerfile CMD
